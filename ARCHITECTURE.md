# Архитектура MVP v0.7
_01.09.2026_

## Пайплайн
Источники → нормализация → дедуп/кластеризация → фильтр релевантности интересам и
рекламы/шума (один LLM-вызов на кластер) → суммаризация прошедших кластеров → доставка
(Telegram-канал)

Архитектурный стиль — **модульный монолит с двумя runtime-компонентами из одного
Python-пакета**:

1. `pipeline-runner` — конечная CLI-команда: запускается по cron в GitHub Actions,
   выполняет один прогон, сохраняет контрольные точки и завершается;
2. `bot-worker` — постоянно работающий процесс с непрерывным Telegram long polling:
   сразу обрабатывает кнопки и команды управления источниками/интересами.

Оба компонента используют один отдельный Neon Postgres-проект AI News Feed через общий
интерфейс `Repository`. Очереди и микросервисы для MVP не нужны: коннекторы,
LLM-провайдер, хранилище и доставка скрыты за небольшими интерфейсами, поэтому их можно
менять независимо от доменной логики.

## Зафиксированный язык и стек

**Выбор: Python 3.12.** Это не только самый короткий путь, но и наименее рискованная
стыковка уже выбранных компонентов:

- Telethon, SemHash, Model2Vec, `pdfplumber`, `python-docx`, `feedparser` и
  `trafilatura` — нативные Python-библиотеки; не понадобится отдельный сервис или FFI
  вокруг каждого шага;
- все CPU-тяжёлые операции внутри этих библиотек выполняются в оптимизированных
  зависимостях, а объём одного личного дайджеста мал, поэтому производительность самого
  Python не является ограничением;
- Python 3.12 поддерживается SemHash (минимум у него — 3.10) и доступен на GitHub Actions;
- тот же язык покрывает коннекторы, извлечение документов, ML и Telegram Bot API, что
  уменьшает число сборок и объём поддержки для соло-проекта.

Фреймворк уровня Django/FastAPI не нужен: у MVP нет HTTP API или веб-интерфейса, а бот
работает через long polling. Оркестрация — обычные Python-модули с `asyncio` и двумя
CLI-точками входа; расписание пайплайна остаётся снаружи в GitHub Actions. Это позволяет
использовать асинхронные Telethon, Bot API, HTTP и Postgres, не добавляя Airflow/Celery.
Если позже появится webhook или UI, HTTP-адаптер можно добавить поверх тех же сервисов.

Базовый набор:

| Задача | Выбор для MVP | Почему |
|---|---|---|
| Проект и зависимости | `pyproject.toml` + `uv`, зафиксированный `uv.lock` | Быстрая воспроизводимая установка локально и в CI |
| Контракты и конфигурация | Pydantic v2 + `pydantic-settings` | Валидация входов коннекторов и структурированного JSON от LLM |
| HTTP/RSS/статьи | `httpx`, `feedparser`, `trafilatura` | Один HTTP-клиент, зрелый разбор фидов и извлечение основного текста |
| Telegram-источники | RSS-Bridge через HTTP; Telethon для fallback и вложений | Простые каналы не требуют своего парсера, сложные остаются доступны |
| PDF/DOCX | `pdfplumber`, `python-docx` | Уже выбранный лёгкий путь без OCR; сканы пока помечаются как ошибка извлечения |
| Дедуп | `hashlib`/нормализация + SemHash/Model2Vec | Сначала почти бесплатный точный фильтр, затем локальная семантика |
| LLM | Собственный тонкий `LLMClient` над SDK выбранного API | Не связывать домен с провайдером и не тянуть LangChain ради двух промптов |
| Bot API | `python-telegram-bot`, непрерывный long polling | Асинхронные handlers, немедленная реакция в личке и готовая отправка сообщений без публичного HTTP endpoint |
| Хранилище | отдельный Neon Postgres 18 по TLS через SQLAlchemy 2 Core + `psycopg` 3; Alembic для миграций | Одно долговечное состояние для bot-worker и cron, полностью изолированное от БД `rus-ai.com` |
| Качество | `pytest`, `ruff`, `mypy` | Быстрые тесты, формат/линт и проверка контрактов в CI |

Альтернативы, которые рассмотрены, но не выбраны:

- **TypeScript**: сильная типизация и удобный Bot API, но Telethon, SemHash и
  `pdfplumber` пришлось бы выносить в Python-сервис. Для MVP это два рантайма и лишняя
  граница отказа.
- **Go**: хороший единый бинарник и дешёвый запуск, но выбранный ML/document stack всё
  равно остаётся в Python; экономия рантайма не окупает обвязку.
- **LangChain/Airflow**: дают провайдерские интеграции и DAG UI, но для линейного
  расписанного прогона добавляют большой граф зависимостей и инфраструктуру. Тонкие
  интерфейсы и GitHub Actions покрывают текущий сценарий.

## Структура Python-проекта

```text
pyproject.toml
alembic/                       # версионируемые миграции Postgres
.github/workflows/pipeline.yml # cron: pipeline-runner только на GitHub runner
deploy/systemd/
  ai-news-feed-bot.service     # отдельный unit-шаблон для VPS
src/ai_news_feed/
  domain/models.py             # типы данных и инварианты, без I/O
  config.py                    # настройки из env/БД
  sources/base.py              # протокол SourceConnector
  sources/rss.py               # нативный RSS + выдача RSS-Bridge
  sources/site.py              # универсальный CSS-скрапер сайтов без RSS
  sources/telegram.py          # web-preview + Telethon
  sources/presets.py           # проверенные адреса/настройки источников ★
  sources/manual.py            # ссылка/forward, присланные боту
  extraction/content.py        # RawItem -> ExtractedItem/ExtractionFailure
  extraction/documents.py      # PDF/DOCX -> текст
  normalization.py             # RawItem -> Material
  dedup/exact.py               # дешёвый фильтр по content_hash
  dedup/semantic.py            # SemHash и сборка кластеров
  llm/base.py                  # тонкий Protocol LLMClient
  llm/openai.py                # Responses API + Structured Outputs
  screening.py                 # релевантность + реклама/шум
  summarization.py             # саммари принятого кластера
  processing.py                # композиция шагов 4–8 без persistence
  digest.py                    # форматирование и лимиты Telegram
  delivery/telegram.py         # Bot API
  storage/base.py              # протокол Repository
  storage/memory.py            # in-memory реализация для тестов
  storage/tables.py            # SQLAlchemy Core metadata
  storage/postgres.py          # PostgresRepository и транзакции
  bot/handlers.py              # управление источниками/интересами в личке
  bot/app.py                   # постоянно работающий long-polling bot-worker
  orchestration/pipeline.py    # порядок шагов, retry, метрики прогона
deploy/
  .env.example                 # шаблон без реальных секретов
  README.md                    # ручной hand-off Neon/VPS/GitHub Actions
  systemd/ai-news-feed-bot.service
tests/
  unit/
  integration/
  fixtures/
```

Модули не передают друг другу произвольные `dict`: публичные границы используют модели
ниже. SQLAlchemy rows и запросы наружу из `storage` не выходят.

## Контракты данных

- `SourceConfig`: `id`, `kind`, `locator`, `collector`, `enabled`, `settings`,
  `cursor`. `collector` явно выбирает `native_rss`, `universal_scraper`, `rss_bridge`,
  `web_preview` или `telethon`, чтобы поведение не менялось от неявной эвристики во
  время прогона. `id`/`source_id` — непустые строки: UUID остаётся допустимым значением,
  но доменный контракт не связывается с выбранным storage-генератором id. `settings` —
  JSON-объект, который каждый коннектор валидирует своей Pydantic-моделью с
  `extra="forbid"`.
- `InterestProfile`: `id`, `name`, `description`, `enabled`, `version`, `created_at`,
  `updated_at`, `updated_by_telegram_user_id`. `description` — непустой свободный текст
  с одной или несколькими темами; система не заставляет пользователя раскладывать его по
  заранее заданной таксономии. Профиль относится к общей ленте/каналу, а не к отдельному
  читателю. Bot-worker позволяет посмотреть и заменить текст или включить/выключить
  профиль; каждое изменение атомарно увеличивает `version`. `pipeline-runner` фиксирует
  `id` и `version` профиля в снимке прогона, а `ClusterScreener` получает `description`
  без скрытого преобразования — так результат можно воспроизвести после редактирования.
- `RawItem`: `source_id`, `external_id`, `original_url`, `published_at`, `title?`,
  `raw_text?`, `raw_html?`, `attachments[]`, `metadata`. Это максимально близкий к
  источнику результат; неполные поля допустимы. Первые четыре поля обязательны,
  `published_at` обязан содержать timezone и при валидации приводится к UTC,
  `metadata` допускает только JSON-значения. Для Telegram `external_id` во всех
  транспортах — десятичный `message_id` без имени канала; уникальность всё равно
  задаётся парой `(source_id, external_id)`.
- `Attachment`: `kind`, `name`, `size?`, `download_ref?`, `mime_type?`. Отсутствующий
  `download_ref` у документа из RSS-Bridge — сигнал маршрутизировать источник в Telethon,
  а не пытаться извлекать пустое вложение. В текущем batch-рантайме Telethon записывает
  документ в выделенный временный каталог и возвращает абсолютный локальный путь как
  `download_ref`; байты не помещаются в Pydantic-модель и БД.
- `CollectionCursor`: строго одна из двух форм — `(published_at, external_id)` для
  RSS/сайтов или `message_id` для Telegram независимо от его транспорта. Частично
  заполненный или смешанный cursor невалиден.
- `CollectionError`: `source_id`, машинный `code`, человекочитаемый `message`,
  `external_id?`, `retryable`. Сетевые/парсинговые сбои возвращаются в batch и не
  отменяют успешно разобранные элементы; неверная комбинация `kind`/`collector` или
  неверные `settings` считается ошибкой конфигурации и отклоняется до I/O.
  `message` требует минимум 1 символ; коннекторы формируют его через
  `error_message()` (`sources/_shared.py`), а не
  голым `str(exc)` -- у части исключений (`TimeoutError`/`asyncio.TimeoutError` без
  аргументов, отдельные внутренние ошибки httpx/Telethon) `str(exc) == ''`, а это
  роняет саму конструкцию `CollectionError` вторичной `pydantic.ValidationError` и
  прерывает весь `run()`/`run_backfill()` вместо одной ошибки по одному источнику
  (найдено на живом месячном backfill 02.09.2026).
- `CollectionBatch`: `raw_items[]`, `next_cursor?`, `errors[]`. Явный аргумент `cursor`
  в `collect()` имеет приоритет над `SourceConfig.cursor`, что позволяет безопасно
  повторить batch с уже загруженным снимком конфигурации; коннектор cursor не сохраняет.
- `Material`: `id`, `source_id`, `external_id`, `original_url`, `published_at`,
  `fetched_at`, `title`, `text`, `language?`, `content_hash`, `metadata`. `text` уже
  очищен от HTML и достаточен для дедупа/LLM; пустой текст запрещён. `Normalizer`
  приводит Unicode к NFKC, стабилизирует пробелы, канонизирует URL (в том числе удаляет
  fragment и известные tracking-параметры) и считает `content_hash` по нормализованному
  регистронезависимому тексту, но сохраняет читабельный регистр в `Material.text`.
- `DuplicateLink`: `material_id`, `duplicate_of_id`, `kind` (`exact_content` или
  `semantic`), `similarity` 0..1. Exact-дубли не теряются: ссылка позволяет вернуть их
  материалы в итоговый кластер, чтобы `DigestItem` сохранил все исходники.
- `ExactDeduplicationResult`: `unique_materials[]`, `duplicate_links[]`.
- `NewsCluster`: `id`, `material_ids[]`, `representative_id`,
  `similarities{material_id: score}`. Состав непустой, представитель обязан входить в
  него, а similarity представителя равна 1. Все материалы кластера сохраняются, даже
  если для промпта используется представитель.
- `ClusterBatch`: `clusters[]`, `duplicate_links[]`; кластеры не содержат только старые
  материалы lookback-окна и создаются лишь при наличии хотя бы одного нового материала.
- `ScreeningResult`: `cluster_id`, `relevance_score` и `noise_score` в диапазоне 0..1,
  `uncertain`, `reason`, `model`, `prompt_version`. Решение `accepted` вычисляет наш код
  по порогам, а не LLM — тогда пороги можно менять без повторного вызова. Явное
  `uncertain=true` всегда пропускает кластер дальше: сомнение не маскируется подбором
  числового score. Невалидный JSON — типизированная ошибка LLM, а не «сомнение».
- `DigestItem`: `cluster_id`, `summary`, `source_links[]`, `model`, `prompt_version`.
  Список ссылок обязателен и содержит URL каждого материала кластера, включая exact-
  дубли. Модель не получает задачу генерировать ссылки; код отвергает сгенерированный URL
  в summary и присоединяет `Material.original_url` самостоятельно. Опциональный
  `source_published_ats[]` (по одной дате на ссылку, тот же порядок) заполняется тем же
  кодом из `Material.published_at` и используется один раз, при форматировании поста
  (подробнее — раздел про даты и скрытые ссылки ниже); не персистится и не хранится в
  `digest_items`.
- `DeliveryReceipt`: `digest_id`, `telegram_message_ids[]`, `sent_at`; пустой receipt не
  создаётся, если принятых кластеров нет.
- `RunReport`: `run_id`, времена начала/конца, счётчики по шагам, ошибки по источникам и
  стоимость/токены LLM. Ошибка одного источника не отменяет успешно собранные остальные.

## Модули пайплайна: вход и выход

| Шаг / модуль | Вход | Выход | Важный инвариант |
|---|---|---|---|
| 0. `Pipeline.load_context` | `run_id`, текущее время | активные `SourceConfig[]`, активный `InterestProfile`, пороги | Один транзакционно согласованный снимок конфигурации на весь прогон |
| 1. `SourceConnector.collect` | один `SourceConfig` + cursor | `CollectionBatch(raw_items[], next_cursor, errors[])` | Cursor не сохраняется до записи полученных items |
| 2. `IdentityDeduper` | `RawItem[]` + известные `(source_id, external_id/url)` | новые `RawItem[]`, ссылки на уже известные | Не скачивать статью/файл повторно |
| 3. `ContentExtractor` | новый `RawItem` и его attachments | `ExtractedItem(text, title, metadata)` или `ExtractionFailure` | PDF/DOCX и HTML дают обычный текст; скан без текста не идёт в LLM |
| 4. `Normalizer` | `RawItem` + `ExtractedItem` | валидный `Material` | URL канонизирован, время в UTC, HTML отсутствует, `content_hash` стабилен |
| 5. `ExactDeduper` | новые `Material[]` + сохранённые content hashes | уникальные `Material[]`, duplicate links | Дословная перепечатка отсекается до эмбеддингов |
| 6. `SemanticClusterer` | уникальные `Material[]` + материалы lookback-окна | `ClusterBatch(clusters[], duplicate_links[])` | SemHash `selected_with_duplicates` превращается в явные кластеры; порог конфигурируем |
| 7. `ClusterScreener` | один `NewsCluster`, его материалы, `InterestProfile.description` | один `ScreeningResult` | Ровно один LLM-вызов на кластер; реклама/шум и релевантность в одном JSON |
| 8. `ClusterSummarizer` | принятый кластер и все его материалы | один `DigestItem` | Ровно один вызов на прошедший кластер; ссылки не генерируются моделью, а добавляются кодом |
| 9. `DigestComposer` | `DigestItem[]`, локаль и лимиты Telegram | `Digest(posts[])` или `NoDigest` | Ноль прошедших кластеров означает штатную тишину; посты не превышают лимит API |
| 10. `TelegramDelivery.send` | `Digest` + channel id | `DeliveryReceipt` или типизированная ошибка | Повтор после сбоя не должен повторно публиковать уже подтверждённые части |
| 11. `Repository.finish_run` | результаты шагов, cursor-ы, receipt, `RunReport` | атомарно сохранённое состояние | Cursor и отметка доставки фиксируются только после соответствующего успеха |

`SourceConnector`, `LLMClient`, `Repository` и `DigestDelivery` оформляются как Python
`Protocol`. Для unit-тестов подставляются in-memory реализации; интеграционные тесты
работают на зафиксированных RSS/HTML/Telegram fixtures и отдельной тестовой Postgres-БД
(service container в CI, очистка данных между тестами, весь контейнер эфемерен).

На 01.09.2026 выбран именно Postgres 18 service container в GitHub Actions: он проверяет
настоящие enum/JSONB/FK/check constraints, optimistic update и транзакции и не требует
Docker на Mac PO. Testcontainers дал бы более быстрый локальный feedback, но требует
локальный Docker; mock SQLAlchemy быстрее обоих вариантов, однако не ловит различия
диалекта и поэтому оставлен только на уровне fake Repository.

Граница LLM намеренно мала: `LLMClient.complete(LLMRequest) -> LLMResponse`, где request
содержит system/user prompt и JSON Schema, а response — текст и фактическое имя модели.
Ни LangChain, ни типы конкретного провайдера в screening/summarization не проникают.
`AIProcessor` композирует шаги 4–8 и читает `InterestProfile`/lookback через `Repository`,
но не сохраняет состояние: транзакция, cache и cursor остаются ответственностью шага 11.
До готовности Postgres этот срез проверяется на `InMemoryRepository` и fake `LLMClient`.

Точная граница источников асинхронна и одинакова для всех транспортов:

```python
class SourceConnector(Protocol):
    async def collect(
        self,
        config: SourceConfig,
        cursor: CollectionCursor | None = None,
    ) -> CollectionBatch: ...
```

Реализация границы источников:

- `collector` явно выбирает транспорт: `native_rss` читает feed напрямую,
  `universal_scraper` вызывает универсальный HTTP/CSS-скрапер, `rss_bridge` — локальный
  RSS-Bridge, `web_preview` — прямой `t.me/s/...`, `telethon` — уже аутентифицированный
  клиент пользовательского аккаунта. Ни один коннектор не делает скрытый fallback;
- RSS и универсальный скрапер возвращают `(published_at, external_id)` cursor; все
  Telegram-коннекторы — `message_id`. `next_cursor` вычисляется, но его сохранение —
  ответственность шага 11 после записи batch;
- универсальный скрапер сначала находит detail URL по `link_selector` и регулярным
  выражениям include/exclude, затем получает каждую статью. Заголовок и дата читаются
  из настроенных селекторов, OpenGraph/`<time>`/JSON-LD; поддержана русская текстовая
  дата. Полный HTML остаётся в `RawItem.raw_html`, основной текст извлекает общий
  `ContentExtractor` через `trafilatura`;
- RSS-коннектор по умолчанию получает полный HTML статьи. Если detail URL временно
  недоступен (в частности, возможный 403 TAdviser), он сохраняет item с HTML-анонсом из
  feed и добавляет retryable `article_fetch_failed`, не теряя саму новость;
- Telegram preview разбирает только реально присутствующие `.tgme_widget_message`.
  Пустая страница возвращает `preview_unavailable`; документ из preview сохраняется как
  `Attachment` без выдуманного `download_ref`;
- Telethon скачивает документы в каталог запуска, заполняет имя/размер/MIME/path, а
  `ContentExtractor` объединяет подпись поста с текстом PDF/DOCX. PDF без текстового
  слоя даёт типизированный failure; OCR в MVP не добавляется.

### Явно рассмотренные развилки в коннекторах

1. **Сайт без RSS: RSS-Bridge CssSelectorBridge или свой скрапер.** Реализован свой
   конфигурируемый `UniversalSiteConnector`: он тестируется без Docker и не привязывает
   модель настроек к query-параметрам RSS-Bridge. Альтернатива остаётся полезной, если
   layout потребует headless/browser-fetch; она подключается отдельным
   `collector=rss_bridge`, не меняя `RawItem`.
2. **Обычный Telegram: только RSS-Bridge или прямой preview.** Оставлены оба явных
   транспорта. RSS-Bridge предпочтителен в production ради своего cache, прямой preview
   удобен для локальной проверки и малого числа каналов. Автоматически переключаться
   между ними нельзя — иначе один и тот же прогон зависел бы от состояния внешнего
   сервиса.
3. **Вложения: байты в `RawItem`, object storage или локальная ссылка.** Для конечного
   `pipeline-runner` выбран локальный абсолютный `download_ref`: это самый дешёвый
   вариант, он не раздувает JSON/БД и живёт достаточно долго до шага extraction.
   Object storage понадобится только при разделении collection/extraction на разные
   процессы; тогда меняется резолвер `download_ref`, а не контракт `Attachment`.
4. **`SourceConfig.id`: обязательный UUID или непрозрачная строка.** Выбрана непрозрачная
   непустая строка, чтобы коннекторы не зависели от Postgres. Repository может выдавать
   UUID и передавать его строковое представление без изменения протокола.

### Идемпотентность и окна

- `Material.id` стабилен: SHA-256 от `(source_id, external_id)`; `RawItem.external_id`
  обязателен текущим контрактом, а `content_hash` хранится отдельно.
- Для каждого источника хранится cursor (`published_at` + external id либо Telegram
  message id). Повторный прогон безопасен и не зависит только от даты из RSS.
- Семантический дедуп сравнивает новый материал как минимум с текущим batch и коротким
  lookback-окном сохранённых материалов. Сейчас defaults — 7 дней и threshold 0,90;
  обе величины конфигурируемы и должны быть откалиброваны на первых реальных примерах.
- Результаты screening/summarization кешируются по `(cluster content hash,
  prompt_version, model)`. Изменение промпта не переиспользует старый ответ.
- LLM получает ограниченный по длине, детерминированно собранный текст. При слишком
  длинном PDF используется отдельный chunk/map-reduce extractor; это отдельная ветка
  внутри `ClusterSummarizer`, а не новый тип материала.

## Технические решения под бюджет ~$50
- **Планировщик**: GitHub Actions (cron). Бесплатных минут хватает на периодический запуск
  `pipeline-runner`. Он намеренно не переносится на VPS: прогон эпизодический, а локальные
  эмбеддинги/извлечение документов могут создавать пики CPU/RAM и не должны отнимать
  ресурсы у `rus-ai.com`. Постоянно на VPS работает только лёгкий `bot-worker`.
- **Хранилище**: отдельный Neon-проект AI News Feed с Postgres 18 и обязательным TLS.
  Провайдер тот же, что у `rus-ai.com`, но project id, connection string, роли, база и
  квоты отдельные; общую базу или schema двух приложений не используем. Доступ —
  через асинхронный SQLAlchemy 2 Core и `psycopg` 3 по собственному `DATABASE_URL`, схема
  меняется только миграциями Alembic. SQLAlchemy Core выбран вместо ORM: доменные
  Pydantic-контракты остаются независимыми от persistence-моделей, запросы и границы
  транзакций видны внутри `PostgresRepository`, а миграции всё равно получают единый
  `MetaData`. `psycopg` 3 — современный драйвер с нативным asyncio; одного драйвера
  достаточно обоим runtime-компонентам.
- **Управление источниками в боте**: инлайн-кнопки «Добавить источник» /
  «Посмотреть все источники» / «Удалить источник» (Telegram Bot API `InlineKeyboardMarkup`
  + `callback_query`). Тип источника (сайт или Telegram-канал) предлагается автоматически
  по формату ввода (URL или @handle), а фактически выбранный `collector` сохраняется в
  `SourceConfig`; так проверенный Telethon-маршрут не сменится обратно на RSS-Bridge.
  Таблица источников — с уникальным ограничением на нормализованный URL/handle, чтобы один
  источник нельзя было добавить дважды; у каждого источника свой id для управления.
- **Обработка команд бота**: отдельный `bot-worker` непрерывно выполняет Telegram long
  polling через `python-telegram-bot` и пишет источники и `InterestProfile` через тот же
  `PostgresRepository`, который читает пайплайн. Это даёт немедленный ответ без публичного
  URL, TLS/webhook-конфигурации и веб-фреймворка. Он разворачивается на имеющемся VPS как
  отдельный systemd-сервис с автоматическим restart; бесплатный PaaS для процесса не нужен.
- **Дедупликация**: сначала SHA-256 `content_hash` по NFKC/casefold/схлопнутым пробелам
  убирает точные перепечатки до эмбеддингов. Затем
  [SemHash](https://github.com/MinishLab/semhash) 0.4.x группирует текущий batch вместе
  с lookback по локальным Model2Vec-эмбеддингам. Англоязычный default SemHash для нашей
  русской ленты не подходит, поэтому encoder лениво загружает
  `minishlab/potion-multilingual-128M`; тесты используют маленький детерминированный fake
  encoder и не скачивают веса. Для личного объёма выбран точный `Backend.BASIC`: он
  детерминирован; если объём вырастет, тот же SemHash переключается на USearch без смены
  контрактов.
- **Фильтр релевантности и рекламы/шума**: один LLM-вызов на кластер (не на статью),
  объединяющий две проверки — «это реклама/шум» и «это по теме интересов пользователя»
  (интересы — свободный текст, передаётся в промпт). Модель — самая дешёвая/быстрая из
  подходящих, это классификация, а не генерация. Результат — числовая оценка
  релевантности, а не только да/нет, чтобы порог можно было потом крутить без переделки
  шага. Начальная политика: `relevance_score >= 0,60`, `noise_score <= 0,70` либо
  `uncertain=true`; пороги конфигурируемы. При сомнении лучше пропустить кластер дальше,
  чем отбросить — для узких тем пропущенная релевантная новость обходится дороже лишней
  в дайджесте. Если ни один кластер не прошёл — прогон завершается без отправки сообщения,
  это ожидаемое поведение.
- **Суммаризация**: один платный LLM-вызов на кластер, прошедший фильтр релевантности, а
  не на каждую статью — самый дорогой шаг, поэтому и самый чувствительный к контролю
  стоимости; лишних вызовов дорогой модели на нерелевантные кластеры при этом не будет.
  Все материалы сериализуются детерминированно с общим лимитом длины, а ссылки добавляет
  код после ответа модели.

### Неоднозначности, оставленные конфигурацией

1. **SemHash threshold**: 0,90 — безопасная стартовая точка; 0,85 даст меньше дублей в
   дайджесте, но чаще склеит соседние новости, 0,93–0,95 консервативнее, но пропустит
   больше перефразированных повторов. Выбрать по размеченной выборке реальных источников.
2. **Lookback**: 7 дней покрывает типичный повтор новости; 3 дня дешевле, 14 дней лучше
   для долго расходящихся отчётов, но повышает риск склейки обновлений одной истории.
3. **Screening policy**: текущие 0,60/0,70 оптимизированы на recall и дополняются
   `uncertain`. После первых дайджестов можно поднять relevance или снизить noise, не
   повторяя LLM-вызовы для уже сохранённых `ScreeningResult`.
4. **ANN backend**: `BASIC` выбран для малого и воспроизводимого MVP; USearch — вариант
   при росте lookback до тысяч/миллионов элементов. Векторная БД для этого не нужна.
- **Доставка**: приватный Telegram-канал — бот добавлен админом с правом публикации и
  постит дайджесты из `pipeline-runner` (bot-worker для доставки не нужен). К каналу
  привязана группа-обсуждение (Discussion) — даёт комментарии под постами штатной функцией
  Telegram, без своей разработки. Если бот должен ещё и читать комментарии (например, как
  сигнал для ранжирования в будущем) — добавить его и в группу-обсуждение, и отключить
  Privacy Mode через @BotFather (`/setprivacy`) до того, как он туда добавлен.
- **Источники**: нативный RSS читается напрямую. RSS-Bridge используется как внешний
  адаптер для сайтов без RSS и обычных публичных Telegram-каналов; Python получает его
  Atom/RSS-выдачу через тот же `feedparser`. Для источников, где веб-превью пусто или
  нужны скачиваемые документы, `collector=telethon` выбирается сразу, а не после тихого
  fallback внутри прогона.
- **Вложения-документы в Telegram (PDF/DOCX-отчёты)**: у части каналов (например,
  аналитика по рынку ИИ) контент лежит не в тексте поста, а во вложенном файле.
  RSS-Bridge отдаёт только имя и размер файла, не сам файл — для таких каналов нужен
  Telethon: скачать документ и извлечь текст (PDF — `pdfplumber`, DOCX —
  `python-docx`), дальше этот текст идёт в тот же фильтр релевантности и суммаризацию,
  что и обычная статья. Для длинных отчётов, возможно, понадобится отдельный промпт
  суммаризации (ключевые цифры/выводы, а не пересказ в одну строку) — решим по факту
  первых тестов.

## Проверка источников ★ (31.08.2026)
Docker недоступен локально на Mac PO — вместо разворачивания RSS-Bridge через него,
проверили напрямую тот же механизм, что использует RSS-Bridge (публичное веб-превью
`t.me/s/<канал>` и HTML/RSS сайтов). RSS-Bridge остаётся в плане для продакшена — на
раннерах GitHub Actions Docker есть по умолчанию, локальный Docker для этого не нужен.

Находки:
- **@ailev_blog** (обычный текстовый канал) — веб-превью отдаёт полный текст постов.
  Подход «RSS-Bridge / веб-превью» подтверждён для этого типа каналов.
- **@expertosphere** — веб-превью (`t.me/s/expertosphere`) не отдаёт посты вообще, только
  шапку канала («Preview channel / View in Telegram») — ни через прямой фетч, ни через
  обычный браузер. Это сильнее исходной находки про вложения (RSS-Bridge отдал бы только
  имя/размер файла, не содержимое) — для этого канала Telethon нужен не как опция для
  вложений, а как единственный рабочий способ вообще получать посты.
- **ict.moscow/analytics** — нативного RSS нет, нужен универсальный CSS-selector-бридж
  RSS-Bridge или свой скрапер. Реализован `UniversalSiteConnector`; preset использует
  `a[href^="/analytics/"]` плюс include/exclude URL-regex, поэтому служебные ссылки и
  фильтры не становятся материалами. Внешняя среда разработки 31.08.2026 не дождалась
  ответа сайта ни через browser, ни через 30-секундный HTTP-запрос; preset проверен на
  зафиксированной реальной структуре/заголовках, а первый production-прогон должен
  подтвердить живую разметку и при необходимости поправить только `settings`.
- **tadviser.ru** — есть RSS: `https://www.tadviser.ru/xml/tadviser.xml` (общий по всему
  сайту, не только по ИИ) — можно читать напрямую (`feedparser`), фильтрация по теме всё
  равно идёт через шаг релевантности. Отдельно: прямой фетч этой страницы через
  автоматический HTTP-клиент вернул 403 (похоже на блокировку по User-Agent/IP), обычный
  браузер открыл страницу без проблем — см. риск ниже.
- **issek.hse.ru/ai** — есть RSS, но общий по организации:
  `https://www.hse.ru/rss/orgs/70333/news_and_announcements.rss`, не только по серии «ИИ» —
  тоже читаем напрямую, фильтруем релевантностью.

Реализация коннекторов закрепляет эти находки тестовыми fixtures: два нативных RSS,
Atom-выдача TelegramBridge, обычный `t.me/s` preview, пустой preview `@expertosphere`,
страница списка/detail ICT.Moscow, Telethon-документ и извлечение настоящих PDF/DOCX.

## Сверка с auto-news и RSS-Bridge (31.08.2026)

Бегло просмотрены `main` и ключевые файлы
[auto-news](https://github.com/finaldie/auto-news): `dags/news_pulling.py`,
`src/af_pull.py`, `src/af_save.py`, `src/ops_rss.py`, `src/llm_agent.py`,
`src/llm_prompts.py` и `pyproject.toml`.

Что берём как референс:

- явные стадии `pull → process/save → publish`, отдельный `run_id` и сохраняемые JSON
  между стадиями — полезный образец наблюдаемости и повторного запуска;
- одинаковый жизненный цикл источников и отдельные source operators;
- кеширование LLM-ответов и сохранение статистики прогона;
- дешёвый pre-filter до LLM для источников с большим потоком.

Что намеренно не переносим:

- Airflow, Notion, Redis, Milvus/Chroma, LangChain/LlamaIndex и десятки опциональных
  зависимостей: это оправдано масштабом auto-news, но слишком тяжело для нашего личного
  cron-процесса и бюджета;
- source operator, который одновременно получает, дедуплицирует, суммаризирует,
  ранжирует и публикует. У нас эти обязанности разделены общими модулями и контрактами;
- обработку RSS независимо от Telegram и документов: наш единый `Material` нужен именно
  затем, чтобы downstream не знал способ получения;
- суммаризацию отдельных статей. В нашем PRD сначала формируется кластер, затем выполняются
  один screening-вызов и один summary-вызов на прошедший кластер.

По исходникам [RSS-Bridge](https://github.com/RSS-Bridge/rss-bridge) подтверждено:

- `TelegramBridge` строит URL `https://t.me/s/<username>`, парсит доступные в публичном
  превью сообщения и выдаёт `uri`, `title`, `content`, `timestamp`, `author`,
  `enclosures`; его часовой cache не делает недоступный пост доступным;
- если `t.me/s/...` не отдаёт элементы сообщений, bridge тоже не сможет получить посты.
  Поэтому проверенный пустой preview `@expertosphere` однозначно маршрутизируется в
  Telethon;
- `processAttachment()` действительно добавляет только имя документа и дополнительную
  строку вроде размера, без URL скачивания. Для извлечения PDF/DOCX нужен Telethon;
- `CssSelectorBridge` может найти ссылки CSS-селектором и опционально сходить на каждую
  страницу за полным контентом. Это подходит для `ict.moscow`, но селекторы хрупкие, а
  расширение каждой статьи медленнее; конфигурация селекторов должна храниться в
  `SourceConfig.settings` и проверяться интеграционным fixture-тестом.

Итог сверки: RSS-Bridge остаётся адаптером только там, где его выдача проверена;
Telethon — полноценный коннектор, а не аварийная деталь внутри RSS-коннектора. Из
auto-news заимствуем контрольные точки и метрики, но оставляем линейный лёгкий рантайм.

## Эксплуатационная топология

### Общее состояние: отдельный Neon-проект

PO должен вручную создать новый Neon-проект **AI News Feed** с Postgres 18 и TLS. Он
может находиться у того же
провайдера, что и база `rus-ai.com`, но не использует её project id, connection string,
database, role или schema. Такая граница исключает случайные миграции/удаления данных
рабочего сервиса и позволяет независимо удалить, восстановить или перенести пет-проект.

На [Neon Free](https://neon.com/pricing) лимиты считаются на проект: AI News Feed получает
собственные 100 CU-часов в месяц и 0,5 ГБ storage, не расходуя квоту проекта `rus-ai.com`.
Для ожидаемого объёма MVP этого достаточно и стоимость остаётся $0, пока проект
укладывается в лимиты; usage проверяется отдельно в Neon Console. Оба runtime-компонента
получают connection string нового проекта через свой secret `DATABASE_URL` и требуют TLS.

`bot-worker` выполняет короткие транзакции изменения источников/интересов, а
`pipeline-runner` в начале прогона читает согласованный снимок конфигурации и дальше
сохраняет материалы, кластеры, решения и доставку через тот же `PostgresRepository`.
Миграции Alembic запускаются отдельной командой до старта новой версии приложения, не
автоматически из двух процессов. Для bot-worker используется маленький connection pool
с `pool_pre_ping`; конечный pipeline-runner не держит соединения после завершения.

Реализованная схема хранит `sources`, `interest_profiles`, `materials`, `news_clusters` +
явное членство `cluster_materials`, `duplicate_links`, versioned `screening_results`,
`digests`, `digest_items` и checkpoint-строки `digest_posts`. Нормализованный locator
источника имеет unique constraint; удаление источника в UI — soft delete, поэтому история
материалов не уничтожается, а повторное добавление восстанавливает ту же запись. Изменение
профиля выполняется `UPDATE ... WHERE version = expected_version` с атомарным `version+1`.

### Bot-worker: systemd на VPS

`bot-worker` размещается на имеющемся VPS (Ubuntu 24.04, 2 vCPU, 4 ГБ RAM, 50 ГБ SSD),
где уже работает `rus-ai.com` (Streamlit + Nginx + HTTPS). Long polling не открывает
входящий HTTP-порт, поэтому бот не меняет Nginx-конфигурацию и не использует домен или
TLS-сертификаты `rus-ai.com`.

Изоляция deployment:

- отдельный непривилегированный OS-пользователь без login shell и sudo, например
  `ai-news-feed`; сервис не запускается от root и не использует пользователя Streamlit;
- отдельный каталог приложения и собственный virtualenv, не общий с Streamlit-проектом;
- отдельный unit `/etc/systemd/system/ai-news-feed-bot.service` с `User=ai-news-feed`,
  своим `WorkingDirectory`/`EnvironmentFile`, `Wants`/`After=network-online.target`,
  `Restart=always`, `RestartSec` и `WantedBy=multi-user.target`; unit включён через
  `systemctl enable`;
- bot token и Neon `DATABASE_URL` лежат в отдельном root-owned environment-файле с
  минимальными правами, не в репозитории и не в unit-файле;
- stdout/stderr идут в journald; deploy/restart бота не перезапускает unit Streamlit.

Для одного bot token работает ровно одна активная реплика bot-worker; webhook отключён,
а offset подтверждённых updates ведёт `python-telegram-bot`. Сервис сразу отвечает на
callback-кнопки и транзакционно обновляет `SourceConfig`/`InterestProfile`. Дубли
источников блокирует уникальный индекс по нормализованному locator, потерянные обновления
профиля — поле `version`.

Systemd restart-policy и собственный VPS закрывают прежний риск, что бесплатный хостинг
усыпит polling-процесс. Остаются обычные риски самого VPS: reboot, нехватка ресурсов и
сетевой сбой. Telegram хранит неполученные updates не дольше 24 часов, поэтому после
перезагрузки unit должен подниматься автоматически. Чтобы защищать `rus-ai.com`, bot-worker
не импортирует ML-модули pipeline-runner; после измерений unit можно ограничить через
systemd `MemoryMax`/`CPUQuota`.

Bot-worker принимает команды только от `TELEGRAM_OWNER_USER_ID` и работает в private
chat. Кнопки и переходы состояния остаются framework-independent в `bot/handlers.py`;
тонкий `python-telegram-bot`-адаптер только переводит Update/callback в эти вызовы. Поэтому
unit-тесты используют одновременно fake Repository и fake Bot API, не сетевой Telegram.

### Pipeline-runner: только GitHub Actions

Периодический `pipeline-runner` остаётся в GitHub Actions и **не запускается на VPS**.
Это эпизодическая batch-задача, которой не нужен daemon/systemd, зато извлечение PDF и
локальные эмбеддинги могут кратковременно нагружать CPU/RAM. GitHub runner принимает эти
пики на себя и не конкурирует со Streamlit/Nginx за 2 vCPU и 4 ГБ RAM VPS.

Workflow получает отдельный Neon `DATABASE_URL`, Telegram/Telethon secrets, выполняет
один идемпотентный прогон и завершается. VPS хранит только лёгкий bot-worker; код и
`Repository` у двух компонентов общие, а runtime и deployment независимы.

`pipeline-runner` перед сбором возобновляет все незавершённые delivery plan. Новый
delivery plan сохраняется в одной Postgres-транзакции с материалами/кластерами/оценками,
после чего Telegram-посты подтверждаются по одному; source cursor двигается только после
успешной доставки. Это не обещает невозможную distributed exactly-once семантику: при
падении строго между успешным `sendMessage` и записью его message id возможен повтор одной
части, но все уже записанные checkpoint-части повторно не публикуются.

LLM-адаптер использует OpenAI Responses API со Structured Outputs и `store=false`, но
скрыт за прежним `LLMClient`. Точные screening/summary model id обязательны в env и не
зашиты в код. Варианты выбора: одна малая модель дешевле и проще; малая для screening +
более сильная для summary (рекомендуемый баланс); одна сильная для обоих шагов проще для
оценки качества, но дороже. Модель выбирается PO по текущим доступу и цене.

### Открытый вопрос: как запускать RSS-Bridge

1. **Эфемерный контейнер внутри workflow** — контролируемая версия и нет внешнего сервиса;
   минусы: startup на каждом прогоне и отсутствие долговечного cache. Рекомендуется для
   первых интеграционных прогонов при малом числе источников.
2. **Публичный instance** — нулевая инфраструктура; минусы: лимиты, приватность списка
   источников и отсутствие SLA. Только для ручной проверки, не как production dependency.
3. **Постоянный self-hosted instance** — работает cache и быстрее повторные запросы;
   минус: возвращает постоянно работающий сервер, которого MVP старается избежать.

Если эфемерный контейнер окажется дороже по минутам или начнёт провоцировать блокировки
без cache, для одного-двух сайтов рациональнее заменить только их на небольшой Python-
скрапер за тем же `SourceConnector`, не менять остальной пайплайн.

## Свежесть, настройки дайджеста и backfill (02.09.2026)

После первого живого прогона обнаружилось: коннекторы ограничивают выдачу только
количеством (`max_items`), а фильтр релевантности (шаг 7) судит исключительно тему, не
дату — старый backlog (2022+ год) свободно проходил в дайджест. Добавлен фильтр свежести в
`AIProcessor.process`: `freshness_cutoff = min_published_at or (now - InterestProfile.
freshness_days)`, применяется до дедупа/кластеризации/LLM-вызовов, а не только на выдаче,
чтобы не тратить на устаревшее ни один вызов модели. `freshness_days` (1–365, по умолчанию
7) — новое поле `InterestProfile`, редактируется в bot-worker («Настройки дайджеста» →
«Изменить свежесть»).

Там же добавлены `summary_length` (`brief`/`normal`/`detailed`, влияет на длину инструкции
в system prompt `ClusterSummarizer`, по умолчанию `normal`) и `tone_instructions` (свободный
текст — промт или примеры стиля, добавляется отдельным блоком в system prompt поверх
базового; `None` — нейтральный редакторский стиль без изменений). Обе настройки — поля
`InterestProfile`, миграция `20260902_01`.

`DigestComposer._pack` больше не объединяет текст разных новостей в один пост: раньше это
был намеренный bin-packing по всем блокам сразу, что при коротких саммари сводило несколько
независимых новостей в один пост. Теперь packing идёт по одному блоку (=одной новости) за
раз; единственная новость, чей текст сам превышает лимит поста, всё ещё делится на
несколько постов через `_split_text` — запрещено именно межновостное объединение, а не
разбиение вообще.

**Backfill («Собрать за период»)** сознательно узок по объёму: `PipelineRunner.
run_backfill(min_published_at)` делает один свежий collect на каждый источник, передавая
копию `SourceConfig` с очищенным cursor — просто передать `cursor=None` в сам вызов
недостаточно, коннекторы всё равно подставляют `config.cursor` (см. `effective_cursor` в
`sources/_shared.py`), а настоящий source в репозитории не трогается до конца прогона.
`max_items` тот же, что и у обычного прогона — не переопределяем, чтобы не упереться в
валидацию каждого коннектора — и код полагается на фильтр свежести выше, чтобы решить, что
из этого одного snapshot'а попадёт в дайджест. Это не глубокая историческая
пагинация: для RSS/сайтов реальная глубина ограничена тем, что отдаёт сам feed/страница
(обычно последние дни-недели независимо от выбранного периода), для Telethon-каналов —
тем же `max_items`, но без даты в самом запросе к Telegram, так что практическая глубина у
Telegram-источников обычно больше. Кнопка периода («неделя» / «месяц» / «год» / «за всё
время») — это выбор `min_published_at`, не гарантия, что источник реально отдаст данные за
этот период. Cursor источника после backfill всё равно продвигается вперёд (как при
обычном прогоне) — это тот же снимок, что увидел бы следующий плановый прогон, так что
дублирования работы дальше не будет. Backfill не резюмирует зависшие («pending») дайджесты
— это остаётся зоной ответственности планового `run()`.

Bot-worker подключает `run_backfill`, только если на старте нашёл те же переменные
окружения, что и pipeline-runner (`OPENAI_API_KEY`, `OPENAI_SCREENING_MODEL`,
`OPENAI_SUMMARY_MODEL`, `TELEGRAM_CHANNEL_ID`, при Telethon-источниках —
`TELEGRAM_API_ID`/`API_HASH`/`SESSION`); без них ядро bot-worker (источники/интересы/
настройки дайджеста) работает как раньше, а кнопка backfill сообщает, что не настроена.
Это осознанный отход от прежнего решения «эти секреты только в GitHub Actions, не на VPS»
(см. `deploy/README.md`) ради мгновенного ответа в чате — исполнение внутри bot-worker было
явно выбрано вместо внешних асинхронных альтернатив (workflow_dispatch/очередь в БД), хотя
долгий callback выполняется фоновой задачей его event loop.
Теперь секреты пайплайна опционально живут и на VPS. Секция по умолчанию закомментирована в
`deploy/.env.example`, так что это осознанный выбор оператора при деплое, а не поведение по
умолчанию.

## Расписание отправки и ручной обычный прогон (03.09.2026)

Сбор по-прежнему запускается GitHub Actions четыре раза в сутки по неизменному cron
`17 1,7,13,19 * * *` UTC. В `InterestProfile.digest_send_times` хранится JSONB-массив
локальных московских меток `{weekday, hour}`: `weekday=null` означает ежедневную метку,
`0..6` — понедельник..воскресенье. Пустой массив сохраняет обратную совместимость и
разрешает отправку в каждом прогоне; схема добавлена миграцией `20260903_01`.

Заданное время — не новый scheduler и не обещание отправки минута-в-минуту. Для каждой
метки `PipelineRunner.run()` находит её последнее наступление в `Europe/Moscow` и
отправляет только если оно попало в полуинтервал между предыдущим и текущим шестичасовым
прогоном: `(now - 6h, now]`. Поэтому, например, ежедневные `09:00` фактически попадут в
первый прогон после 09:00 (при текущем cron — примерно в 10:17 МСК), а следующие прогоны
этого дня уже не удовлетворят окну. Для недельной метки тот же механизм использует
семидневный цикл и корректно работает через границу недели. Такой снэппинг сохраняет
частый сбор, свежие кластеры и простую внешнюю оркестрацию; точный отдельный cron для
каждого пользовательского времени размножал бы scheduler-состояние и позволял бы
материалам устареть между редкими отправками.

Гейт охватывает только вызовы `DigestDelivery.send`, включая возобновление pending-
дайджестов. Сбор, extraction, дедуп, screening, суммаризация, сохранение delivery plan и
продвижение source cursor выполняются в каждом шестичасовом прогоне. Если время ещё не
наступило, готовый digest остаётся pending и отправляется в первое подходящее окно вместе
с новыми результатами, без повторного LLM-процессинга уже сохранённых материалов.

Кнопка главного меню «Прислать всё актуальное» использует тот же `PipelineRunner` и те же
опциональные pipeline-секреты/клиенты, что кнопка backfill, но вызывает обычный
инкрементальный `run(ignore_schedule=True)`: source cursor уважается, pending delivery
возобновляется, а `digest_send_times` намеренно обходится как явный запрос владельца
«прислать сейчас». Bot-worker сразу подтверждает запуск и после завершения сообщает числа
проверенных источников, новых материалов и реально отправленных постов, включая явный
нулевой результат. `RUN_NOW` регистрируется как отслеживаемая фоновая задача самого
Telegram Application, чтобы последовательный update loop мог принять повторный callback,
пока долгий pipeline ещё работает. Один процесс держит общий in-flight флаг для ручного
обычного прогона и backfill, поэтому повторное нажатие до завершения получает «Уже
выполняется» и не запускает конкурентную работу над общими cursor/pending-состояниями.

## Даты источников и скрытые ссылки в постах дайджеста (03.09.2026)

После первого живого дайджеста: под каждой ссылкой нужна дата новости, а сама ссылка --
не голый URL, а кликабельный текст. Оба изменения сделаны в `digest.py` (`_format_item`)
и `delivery/telegram.py`.

Дата хранится по каждой ссылке отдельно (`DigestItem.source_published_ats: tuple[datetime,
...] | None`), а не одна дата на весь дайджест или на новость в целом: у кластера может
быть несколько источников (`source_links` уже tuple, см. `test_summary_has_every_source_
link_added_by_code`), и у каждого своя `Material.published_at`. Поле опционально и
намеренно не персистится -- нет колонки в `digest_items`, нет миграции:
`ClusterSummarizer.summarize` заполняет его один раз, при сборке `DigestItem`, тем же
порядком, что и `source_links` (оба tuple строятся из одного `ordered_materials`, так что
расхождение по индексам невозможно даже случайно). `DigestComposer` читает поле один раз,
при форматировании -- результат уже осел в готовых строках `Digest.posts`, а именно они
переотправляются при повторе/чекпоинте доставки (`TelegramDelivery.send`). `DigestItem`,
пересобранный из БД (`postgres.py:list_pending_digests`), это поле не восстанавливает и не
должен -- его текст больше не форматируется заново.

Ссылки скрыты через `parse_mode="HTML"` в `TelegramBotAPI.send_message` (единственный
вызов -- `TelegramDelivery.send`): `<a href="URL">домен</a>` вместо голого URL, подпись --
netloc без `www.` (`_link_label`), а не порядковый номер, чтобы источник был виден не
открывая ссылку. HTML выбран вместо MarkdownV2, потому что у HTML на порядок короче список
символов, которые обязательно экранировать (`<`, `>`, `&` -- против почти всей пунктуации
MarkdownV2: тире, точки, скобки и т.п., которые обычный LLM-текст содержит постоянно).
**Инвариант**: с этого момента любой текст, попадающий в пост дайджеста, обязан пройти
`html.escape()` до интерполяции -- сейчас это `item.summary` (LLM-текст, ничем не
ограничен по символам) и сами URL внутри `href="..."` (могут содержать `&` в
query-параметрах). Если в `_format_item` появится новый источник текста, про экранирование
нужно вспомнить явно -- автоматической защиты для HTML здесь нет (`ContractModel`-валидация
проверяет только формат данных, не безопасность разметки).

Известный неустранённый риск: `_pack`/`_split_text` режут длинный пост по границе символов
и пробелов, не зная о HTML-тегах -- теоретически можно разрезать пост посередине
`<a href="...">`, что сломает парсинг HTML в этом сообщении. Не исправлено: `_pack` делит
пост только тогда, когда текст ОДНОЙ новости (summary до 1500 симв. плюс несколько ссылок с
датами) сам превышает 4096 символов -- редкий случай, решили не усложнять `_split_text`
HTML-осведомлённостью до первого реального инцидента.

### Кнопка «Проверить фильтр»: что реально прошло/не прошло отбор

Нужно было видеть, какие новости бот принимает и отклоняет и почему -- чтобы можно было
судить о качестве фильтра, а не только о его результате в канале. Оказалось, что это чисто
read-only фича: пайплайн уже сохраняет `news_clusters`/`cluster_materials`/`materials` для
КАЖДОГО кластера, прошёл он отбор или нет (см. `orchestration/pipeline.py`, `screening_
results` передаётся в `save_processing_result` без фильтрации по accept/reject), так что не
понадобилось ни новых миграций, ни изменений в самом пайплайне -- только новый метод чтения.

`Repository.list_recent_screenings(profile_id, *, limit=10)` (`storage/base.py`) возвращает
`ScreeningReview` -- read-модель, которая ОБОРАЧИВАЕТ настоящий `ScreeningResult`, а не
копирует его поля, плюс заголовок/URL/дата представителя кластера для отображения. Вердикт
(принято/отклонено) нигде не хранится -- это по-прежнему вычисляемый метод, а не колонка
(см. `screening_results` в разделе "Контракты данных" выше): вердикт считается на лету через
`ScreeningResult.accepted()`/`ScreeningThresholds.accepts()` поверх реальных данных, что
переиспользует ту же логику и константы (0.6/0.7), что применяет сам пайплайн, вместо того
чтобы держать копию порогов в bot-worker.

У кластера может быть больше одной строки `screening_results`, если его пересмотрели под
другой model/prompt_version (составной PK в tables.py) -- Postgres-реализация схлопывает
это через `DISTINCT ON (cluster_id) ... ORDER BY cluster_id, created_at DESC`: наружу идёт
только последняя строка на кластер, а `LIMIT` уже применяется к дедуплицированному набору,
не к сырым строкам. In-memory реализация (только для тестов) устроена проще: `ScreeningResult`
не хранит свою метку времени вообще, так что вместо `created_at` используется порядок
вставки в `dict` как прокси свежести -- этого достаточно для тестов, но это не то же самое,
что настоящее время скрининга.

`BotAPI.send_message` -- одно нечанкованное сообщение (см. пример `_send_sources`), ничего
похожего на `digest.py`'s `_split_text`/`_pack` здесь не переиспользуется. Поэтому вывод
намеренно краткий по просьбе ("всё подряд с вердиктом, но кратко"): не больше 10 последних
кластеров, заголовок и причина обрезаются (`_truncate`, 200/160 симв.), и есть жёсткий
предохранитель на общую длину сообщения (~3800 симв. с запасом до лимита Telegram в 4096) --
если увеличить `_SCREENING_REVIEW_LIMIT` или убрать этот предохранитель, не подумав о длине,
сообщение просто не отправится.

## Что осознанно откладываем
- Векторную БД/pgvector (эмбеддинги и их метаданные хранятся в обычном Postgres,
  поиск и SemHash выполняются локально в pipeline-runner).
- HTTP API и веб-интерфейс (bot-worker использует long polling, pipeline-runner — CLI).
- YouTube/VK-коннекторы и FB-Pages — Фаза 2. FB-группы — не планируем без пересмотра.
- Персонализацию/ранжирование сверх базового — при канале на нескольких человек все видят
  одну и ту же ленту; разная персонализация под разных людей означала бы отдельные каналы.

## Фаза 2 (после MVP)
- **Репост оригинала по метрикам**: помимо дайджеста — прямая ретрансляция поста из
  источника без изменений (`forwardMessage`, сохраняет атрибуцию, либо `copyMessage` +
  ссылка вручную), если просмотры/реакции оригинала выше заданного порога. Метрики
  читаются тем же способом, что и сам контент источника (Telethon/RSS-Bridge) — отдельный
  механизм не нужен.
- **Персонализация по реакциям**: реакции на посты бота читаются через Bot API —
  `message_reaction` в личке/группе (с привязкой к человеку), `message_reaction_count` в
  каналах (только агрегированное число, анонимно). Пока канал на одного человека, этого
  достаточно; если канал станет общим на нескольких людей — привязать реакцию к
  конкретному человеку в канале Telegram не даёт, персонализация на уровне человека в общем
  канале технически не реализуема. Начинаем с простого: вектор предпочтений = взвешенное
  среднее эмбеддингов лайкнутых минус дизлайкнутых кластеров (те же эмбеддинги, что и для
  дедупа), новые кандидаты оцениваются косинусным сходством с этим вектором — без новых
  зависимостей. Более серьёзные варианты, если понадобятся, — в RESEARCH.md (rss-swipr,
  river).

## Риски, за которыми стоит следить
- Отдельный Neon-проект изолирует AI News Feed от `rus-ai.com`, но имеет собственные
  бесплатные лимиты 100 CU-часов/месяц и 0,5 ГБ. Следить за usage и ростом материалов;
  превышение квоты или изменение условий тарифа не должно обнаруживаться только по
  упавшему cron. Scale-to-zero может добавить задержку первому запросу после простоя.
- VPS остаётся общей физической точкой для bot-worker и `rus-ai.com`: контролировать RAM,
  CPU, SSD и размер journald. Bot-worker не должен загружать SemHash/модели; при
  необходимости зафиксировать `MemoryMax`/`CPUQuota`, не ухудшая отзывчивость бота.
- VPS/reboot/сеть — единая точка отказа bot-worker. Unit должен быть `enabled`, иметь
  restart-policy и проверяться простым health-check/алертом; простой дольше окна хранения
  Telegram updates может потерять команды. Риск «бесплатный хостинг усыпил процесс» снят,
  поскольку polling работает на собственном VPS.
- Ошибка прав или утечка environment-файла не должна давать доступ к `rus-ai.com`:
  отдельные OS-user, каталоги, Neon project/role и secrets обязательны, backup и миграции
  AI News Feed выполняются независимо.
- Чтение Telegram через пользовательский аккаунт технически может попадать под
  ограничения Telegram на автоматизацию — риск для личного аккаунта, стоит свериться
  с актуальными правилами перед тем, как полагаться на это постоянно. Решение
  01.09.2026: Telethon-сессия заводится на отдельный (резервный) аккаунт PO, а не на
  основной, которым управляются бот и канал, — это два независимых друг от друга
  Telegram-identity, и разделение их изолирует риск ограничения аккаунта от основного
  личного профиля.
- VK может изменить условия API без предупреждения.
- Парсинг сайтов без RSS менее стабилен, чем официальные API.
- Рост стоимости LLM-вызовов при увеличении числа источников — контролировать
  суммаризацией по кластерам и дешёвыми моделями на шаге дедупа/фильтрации.
- Некоторые сайты блокируют автоматические HTTP-клиенты (tadviser.ru вернул 403
  прокси-фетчеру, но открылся в обычном браузере) — если повторится в проде (GitHub
  Actions), может понадобиться другой User-Agent, задержки между запросами или
  headless-браузер вместо простого HTTP-запроса. Проверить на первом реальном прогоне.
- Reasoning-модели (например, gpt-5-nano) могут потратить весь `max_output_tokens` на
  внутреннее рассуждение и не выдать видимый ответ (`status=incomplete`, в `output`
  только reasoning-элементы, без message/output_text) — подтверждено эмпирически
  01.09.2026 через scripts/smoke_test_openai.py. Отдельно: `AIProcessor.process()` не
  изолирует ошибку одного кластера — сбой LLM-вызова на любом кластере роняет весь
  прогон целиком, а не только этот кластер. Для reasoning-моделей в screening/summary —
  закладывать больший `max_output_tokens` и/или явный `reasoning.effort`, либо не
  использовать их здесь, пока не появится per-cluster изоляция ошибок.
